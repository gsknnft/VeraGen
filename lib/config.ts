// Free-tier export cap. Paid tiers unlocking longer exports are a phase-2
// concern (see SCOPE.md) — this constant is the single gate to raise once
// that billing layer exists.
export const FREE_EXPORT_MAX_SECONDS = 15;
